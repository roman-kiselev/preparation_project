/**
 * Задача 1
 * Расчитывает индекс массы тела (ИМТ) и определяет категорию веса.
 *
 * ИМТ (индекс массы тела) рассчитывается по формуле:
 * масса тела в килограммах / (рост в метрах)^2.
 *
 * Функция возвращает массив, где первый элемент — это ИМТ,
 * а второй элемент — категория веса.
 *
 * Категории:
 * - недостаток веса: ИМТ < 18.5
 * - нормальный вес: 18.5 ≤ ИМТ < 25
 * - избыточный вес: 25 ≤ ИМТ < 30
 * - ожирение: ИМТ ≥ 30
 *
 * ИМТ должен быть округлен до двух знаков после запятой.
 *
 * @param {number} weight - Вес в килограммах.
 * @param {number} height - Рост в сантиметрах.
 * @returns {[number, string]} Массив с ИМТ и категорией веса.
 *
 * @example
 * calculateBMI(70, 170); // [24.22, 'нормальный вес']
 * @example
 * calculateBMI(30, 150); // [13.33, 'недостаток веса']
 * @example
 * calculateBMI(90, 180); // [27.78, 'избыточный вес']
 * @example
 * calculateBMI(100, 180); // [30.86, 'ожирение']
 */
